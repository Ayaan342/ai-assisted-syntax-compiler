float readings[3];

int main() {
    char grade = 'A';
    float scale = 3.14;
    bool enabled = true;
    readings[0] = scale;
    // Strings are lexically supported for future expression grammar.
    "hello";
    return 0;
}

